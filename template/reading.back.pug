doctype html
main(class=`${color} ${mode} back`)
  .top
    |
    | {{#picture}}
    .picture {{picture}}
    | {{/picture}}
    .text
      | {{#kanji}}
      h1(class=`title {{Tags}}`) {{kanji}}
      | {{/kanji}}
      | {{^kanji}}
      h1(class=`title {{Tags}}`) {{kana}}
      | {{/kanji}}
      | {{#target}}
      .target {{target}}
      | {{/target}}
  | {{#kanji}}
  h2!= grammar[0]
    span.speech {{speech}}
  h1 {{nederlands}}
  | {{#kana}}
  div {{kana}}
  | {{/kana}}
  | {{/kanji}}
  | {{^kanji}}
  h2!= grammar[1] || grammar[0]
  h1 {{nederlands}}
    span.speech {{speech}}
  | {{/kanji}}
  | {{#notes}}
  div {{notes}}
  | {{/notes}}
  | {{#context}}
  div.context {{context}}
  | {{/context}}
  | {{#strokes}}
  div.strokes {{strokes}}
  | {{/strokes}}
