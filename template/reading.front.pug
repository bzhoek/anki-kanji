include _mixins
+main(color, mode, 'front')
  .top
    +hint('picture')
    .text
      | {{#kanji}}
      h1(class=`kanji {{Tags}}`) {{kanji}}
      | {{/kanji}}
      | {{^kanji}}
      h1(class=`kanji {{Tags}}`) {{kana}}
      | {{/kanji}}
      +hint('target')
  | {{#kanji}}
  h2!= grammar[0]
  | {{/kanji}}
  | {{^kanji}}
  h2!= grammar[1] || grammar[0]
  | {{/kanji}}

