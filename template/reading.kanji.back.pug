include _mixins
+main(color, mode, 'back')
  .top
    +field('picture')
    +text
      +field('strokes')
      +title('nederlands')
      +field('kana')
  +audioheader(grammar[0])
  +fields('notes', 'target')
