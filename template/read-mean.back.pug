include _mixins
+main(color, mode, 'back')
  +kanjikana(grammar)
  +field('picture')
  +optional('#kanji')
    +heading('furigana')
  +optional('^kanji')
    +heading('kana')
  +title('meaning')
  +optional('#kanji')
    +field('kana')
  +field('target')
  +fields('notes', 'strokes', 'details')
  +audio
