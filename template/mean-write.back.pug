include _mixins
+main(color, mode, 'back')
  +kanjikana(grammar)
  +field('picture')
  +title('furigana')
  +title('meaning')
  +fields('notes', 'kana')
  +field('target')
  +audio
  +fields('strokes', 'details')
