include _mixins
+main(color, mode, 'back')
  +kanjikana(grammar)
  +field('picture')
  +title('meaning')
  +fields('notes', 'strokes', 'kana')
  +field('target')
  +audio
  +field('details')
  +title('furigana')
