= '{{#speech}}\n'
include _mixins
+main(color, mode, 'front')
  .top
    +hint('picture')
    .text(class=`{{Tags}}`)
      h1
        span.type= type
      +hint('hint')
  +kanjikana(grammar)
= '\n{{/speech}}'