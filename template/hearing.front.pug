= '{{#speech}}\n'
include _field
+main(color, mode, 'front')
  .top
    +hint('picture')
    .text
      h1(class=`{{Tags}}`)
        span.type= type
      +hint('hint')
  +kanjikana(grammar)
= '\n{{/speech}}'