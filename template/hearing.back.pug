include _mixins
+main(note, color, mode, 'back')
  .top
    +field('picture')
    +text
      +title('furigana')
      +title('meaning')
      +fields('kana', 'target')
  +audioheader(grammar[1] || grammar[0])
  +fields('notes', 'strokes', 'details')
