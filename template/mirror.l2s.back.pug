include _mixins
+main('blue', 'listening', 'back')
  +field('picture', back)
  +heading('reading', back)
  +field('speaking', back)
  +field('strokes', back)
  +field('notes', back)
  +field('listening', back)
  +listening(front)
  +heading('reading', front)
  +field('speaking', front)
  +field('notes', front)
