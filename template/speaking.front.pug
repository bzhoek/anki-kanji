= notempty && `{{#${notempty}}}\n`
|
main(class=`${color} ${mode} front`)
  .top
    |
    | {{#picture}}
    .picture {{hint:picture}}
    | {{/picture}}
    .text
      h1(class=`${clazz} {{Tags}}`)= `{{${fmain}}}`
      | {{#target}}
      .target {{hint:target}}
      | {{/target}}
  h2!= grammar[0]
include scale.pug
|
= notempty && `\n{{/${notempty}}}`
