= notempty && `{{#${notempty}}}\n`
|
main(class=`${color} ${mode} front`)
  .top
    include _picture_hint
    .text
      h1(class=`${clazz} {{Tags}}`)= `{{${fmain}}}`
      include _target
  h2!= grammar[0]
include _scale.pug
|
= notempty && `\n{{/${notempty}}}`
