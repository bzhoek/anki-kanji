= notempty && `{{#${notempty}}}\n`
|
main(class=`${color} ${mode} front`)
  .top
    |
    | {{#picture}}
    .picture {{picture}}
    | {{/picture}}
    .text
      h1(class=`${clazz} {{Tags}}`)= `{{${front}}}`
      | {{#target}}
      .target {{target}}
      | {{/target}}
  h2!= grammar[0]
|
= notempty && `\n{{/${notempty}}}`
