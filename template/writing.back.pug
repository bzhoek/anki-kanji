doctype html
main(class=`${color} ${mode} back`)
  .top
    |
    | {{#picture}}
    .picture {{picture}}
    | {{/picture}}
    .text
      h1(class=`title {{Tags}}`) {{nederlands}}
      | {{#target}}
      .target {{target}}
      | {{/target}}
  h2!= grammar[1] || grammar[0]
    span.speech {{speech}}
  | {{#strokes}}
  div.strokes {{strokes}}
  | {{/strokes}}
  | {{#notes}}
  div {{notes}}
  | {{/notes}}
  div {{furigana}} {{kana}}
  | {{#context}}
  div.context {{context}}
  | {{/context}}
