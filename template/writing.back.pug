doctype html
main(class=`${color} ${mode} back`)
  .top
    |
    | {{#picture}}
    .picture {{picture}}
    | {{/picture}}
    .text
      h1(class=`{{Tags}}`) {{nederlands}}
      | {{#target}}
      .target {{target}}
      | {{/target}}
  h2!= grammar[1] || grammar[0]
    span.speech {{speech}} {{context}}
  | {{#strokes}}
  div.strokes {{strokes}}
  | {{/strokes}}
  | {{#notes}}
  div {{notes}}
  | {{/notes}}
  div {{furigana}} {{kana}}
include radical.pug
