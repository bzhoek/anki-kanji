include _mixins
+main(color, mode, 'back')
  .top
    +field('picture')
    .text
      +title('furigana')
      +title('nederlands')
      +fields('notes', 'kana')
  h2!= grammar[1] || grammar[0]
    include _audio
  +fields('target', 'strokes', 'details')
