main(class=`${color} ${mode} back`)
  |
  h1 {{nederlands}} {{speech}}
  h2!= grammar[1] || grammar[0]
  div {{kanji}}
  | {{#notes}}
  div {{notes}}
  | {{/notes}}
  | {{#strokes}}
  div.strokes {{strokes}}
  | {{/strokes}}
  div {{kana}}
  | {{#context}}
  div.context {{context}}
  | {{/context}}
