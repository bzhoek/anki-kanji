doctype html
main(class=`${color} ${mode} back`)
  .top
    |
    | {{#picture}}
    .picture {{picture}}
    | {{/picture}}
    .text
      h1(class=`{{Tags}}`) {{nederlands}}
      .furigana {{furigana}}
      | {{#target}}
      .target {{target}}
      | {{/target}}
  h2!= grammar[1] || grammar[0]
    span.speech {{speech}} {{context}}
  | {{#notes}}
  div {{notes}}
  | {{/notes}}
  | {{#strokes}}
  div.strokes {{strokes}}
  | {{/strokes}}
  div {{kana}}
