doctype html
main(class=`${color} ${mode} back`)
  .top
    |
    | {{#picture}}
    .picture {{picture}}
    | {{/picture}}
    .text
      h1(class=`title {{Tags}}`) {{speech}}
      | {{#target}}
      .target {{target}}
      | {{/target}}
  h2!= grammar[1] || grammar[0]
  h1 {{nederlands}}
  div {{kanji}}
  | {{#notes}}
  div {{notes}}
  | {{/notes}}
  | {{#strokes}}
  div.strokes {{strokes}}
  | {{/strokes}}
  div {{kana}}
  | {{#context}}
  div.context {{context}}
  | {{/context}}
