doctype html
include _field
main(class=`${color} ${mode} back`)
  .top
    include _picture
    .text
      h1(class=`title {{Tags}}`) {{nederlands}}
  +fields('strokes', 'notes', 'on')
  h2!= grammar[0]
    include _audio
  include _target
include _radical
