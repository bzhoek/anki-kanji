include _mixins
+main(color, mode, 'back')
  .top
    +field('picture')
    .text
      h1(class=`title {{Tags}}`) {{nederlands}}
  +fields('strokes', 'notes', 'on')
  h2!= grammar[0]
    include _audio
  +field('target')
